apiVersion: v1
kind: ConfigMap
metadata:
  name: chains-config
  namespace: system
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: argo-supply-chain-security
    app.kubernetes.io/managed-by: kustomize
data:
  artifacts.signer: tmp
